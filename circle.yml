machine:
  services:
  - docker
  environment:
    ESCAPE_PROJECT: "_"

dependencies:
  pre:
  - curl https://storage.googleapis.com/escape-releases-eu/escape-v0.11.2.tgz > /home/ubuntu/bin/escape.tgz
  - tar -C /home/ubuntu/bin -xvzf /home/ubuntu/bin/escape.tgz

compile:
  override:
  - escape release

test:
  override:
  - echo "Already tested"

